#include "main.h"

/**
 * int _islower(int c) - Check if a character is a lowercase
 * Returns 1 if c is lowercase Returns 0 otherwise
 **/

int _islower(int c)
{
if (c  >= 'a' && c <= 'z')
return (1);
return (0);
}
